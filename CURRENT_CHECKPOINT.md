# CURRENT CHECKPOINT

## Baseline

`main` is the authoritative GitHub baseline.

Latest merged implementation baseline:

`9a4f08207dfce361c7ec27e6b8150f2638b24090`

The `main` branch also contains the checkpoint documentation update at:

`97bdb4ebd7f8d9abc7647a734b6c08a573a8fc6c`

The current open engineering milestone is pull request `#17`, task-scoped Git mutation control plane, based on the checkpoint above.

## Verified architecture

The repository now includes registry-driven leader/worker routing, runtime connection resilience, project understanding, context building, central leadership, plan/decomposition, worker dispatch, guarded worker execution, independent validation, internal execution authorization, resource/process sandboxing, policy-first terminal execution, inspection-only Git terminal safety, and centralized secret/log redaction.

The normal worker process path is:

```text
WorkerExecutionBoundary
  ↓
TerminalExecutor
  ↓
TerminalPolicy
  ↓
GitSafetyPolicy for Git inspection
  ↓
ProcessSandbox
  ↓
SecretRedactor
  ↓
Execution Gate
```

## Task-scoped Git mutation milestone

Pull request `#17` introduces a separate mutation control plane:

```text
Passed ValidationVerdict
        +
Isolated checkpoint
        +
Exact FileChange targets/content
        ↓
ExecutionAuthorizationBoundary
        ↓
GitMutationExecutor
        ↓
GitMutationPolicy
        ↓
ProcessSandbox
        ↓
Git add -- <exact targets>
        ↓
verify staged target set
        ↓
verify current content == validated FileChange.new_text
        ↓
Git commit -m <bounded single-line message>
        ↓
verify HEAD + clean index/worktree + exact committed target set
```

Only local `stage` and `commit` operations are exposed. Remote mutation, history rewriting, destructive cleanup, branch switching, merge/rebase/cherry-pick/stash, configuration injection, amendment, signing overrides, and hook bypass are not exposed.

Every mutation consumes prior independent-validation and internal authorization evidence. It cannot self-authorize.

Target paths must be workspace-relative, non-duplicate, existing regular UTF-8 files with no symlink/junction escape. Current contents must still equal the validated `FileChange.new_text` immediately before mutation begins.

The control plane refuses unrelated working-tree or pre-staged index state, serializes same-workspace mutation through a fail-closed lock, verifies the staged target set before commit, and verifies post-commit HEAD/index/worktree/target-set evidence.

Commit messages are bounded single-line values and are rejected when `SecretRedactor` identifies credential-like material, preventing recognizable credentials from entering Git history.

Mutation failure preserves staged evidence and never runs blind `reset`, `restore`, or `clean` recovery.

## Validation status for PR #17

The first Windows validation run exposed two real implementation defects:

1. `pathlib` normalized `./file.py` before the target policy examined path parts, so explicit traversal syntax was not rejected.
2. `ProcessSandbox` rejected `-m` for every executable, which incorrectly blocked the valid Git commit command generated by the dedicated mutation policy.

These were corrected on the feature branch. Additional hardening then added:

- validated target-content drift rejection;
- credential-like commit-message rejection;
- regression coverage for both protections;
- refreshed architecture, security, rules, handoff, and control-plane documentation.

The corrected branch still requires a fresh real Windows validation run before merge.

## Required Windows gate

```text
python -m compileall -q .
python -m pytest -q test_git_mutation_policy.py test_git_mutation_executor.py test_git_safety.py test_terminal_policy.py test_terminal_executor.py
python -m pytest -q
git diff --check
python config_registry.py
python leader_router.py
python worker_router.py
python orchestration_smoke_test.py
git status --short --branch
```

No successful result is claimed until that gate is executed after the latest branch changes.

## Protected local state

The following remain local-only and must never be deleted, overwritten, truncated, renamed, or cleaned by synchronization:

```text
groq_keys.txt
openrouter_keys.txt
groq_keys.backup.txt
openrouter_keys.backup.txt
```

Other local `.env`, runtime state, machine credentials, caches, and preserved profile backups remain outside the GitHub promotion path.

Never use destructive synchronization such as blind `git clean -fd` or `git reset --hard`.

## Promotion rule

```text
fresh Windows validation
    ↓
diff review
    ↓
security / architecture review
    ↓
merge PR #17 if green
    ↓
update this checkpoint with the actual merge commit
```
